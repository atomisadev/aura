[project]
name = "python-app"
version = "0.1.0"
description = "A small Python workspace app that is launched through Bun."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

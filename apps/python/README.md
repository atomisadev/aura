# Python App

Run from the repo root:

```bash
bun run --filter python run
```

Run from inside `apps/python`:

```bash
bun run run
```

Quick syntax check:

```bash
bun run check-types
```

The Python files live directly in `src/`.
